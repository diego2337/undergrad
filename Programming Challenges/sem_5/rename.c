#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef char enterprises[101];
enterprises empresas[1001];

void read_names()
{
	while()
}

int main(void)
{
	int N, M, i, j;
	scanf("%d", &N);
	for(i = 0; i < N; i++)
	{
		read_names();
	}
	return 0;
}