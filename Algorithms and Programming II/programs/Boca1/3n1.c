#include<stdio.h>
// Programa: 3n1.c
// Programador: Diego Cintra
// Data: 09/11/2012
// O dialogo: Esse programa ira fazer, para o numero par, uma divisao por 2; para um numero impar, multiplicar por 3 e somar 1, imprimindo a sequencia inversa.
int 3n1(int num)
{
	if(num == 1)
	return num;
	
	
	
}
int main(void)
{
	int i, n, num;
	scanf("%d", &n);
	for(i = 0; i < n; i++)
	{
		scanf("%d", &num);
		3n1(num);
		print("%d", 3n1(num);
	}
return 0;
}
